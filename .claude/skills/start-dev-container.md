---
name: start-dev-container
description: Start the HA dev instance via Docker Compose
command: docker compose up -d
---

Starts the Home Assistant development container for testing the integration. Requires Docker to be running. The compose file should be in the ha-hemm repo.
